# Copy to .env only if you extend docker-compose with extra variables.
# Default local URLs (no .env required for basic dev):
#   Editor:  http://localhost:5678
#   Webhook: http://localhost:5678/webhook/<path>
