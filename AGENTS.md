## Agent skills

### Issue tracker

GitHub Issues on `karlnolan567/karlnolan-website` via `gh` CLI. External PRs are not a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary (`ready-for-agent`, etc.). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context; no `CONTEXT.md` yet. See `docs/agents/domain.md`.

### Wayfinder

Foggy-problem maps and tickets on GitHub Issues via `/wayfinder`. See `docs/agents/issue-tracker.md` (Wayfinding operations).
